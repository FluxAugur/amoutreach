<div id="request-invite" class="modal" style="display: <%= @user.errors.any? ? 'block' : 'none';%>">
  <%= simple_form_for resource, :as => resource_name, :url => registration_path(resource_name) , :html => {:class => 'form-horizontal' } do |f| %>
    <div class="modal-header">
      <a class="close" data-dismiss="modal">&#215;</a>
      <h3>Request Invitation</h3>
    </div>
    <div class="modal-body">
      <%= f.error_notification %>
      <%= f.input :email, :placeholder => 'user@example.com' %>
    </div>
    <div class="modal-footer">
      <%= f.submit "Request Invitation", :class => "btn btn-primary", :id => 'invitation_button' %>
      <a class="btn" data-dismiss="modal" href="#">Close</a>
    </div>
  <% end %>
</div>
<% if !@user.errors.any? %>
  <div id="romance-copy" style="text-align: center; margin-top: 80px">
    <%= image_tag("/logo.png") %>
    <h2 style="text-shadow: 3px 3px 1px rgba(0, 0, 0, 0.25);">A+M Outreach will be launching soon!</h2>
    <p>Sign up below to receive all the latest news as we get closer to launch.</p>
  </div>
  <div id="call-to-action" style="text-align: center; margin-top: 80px">
    <a class="btn btn-primary btn-lg" data-toggle="modal" href="#request-invite">Request invite</a>
  </div>
  <div id="countdown" style="center; margin-top: 40px;">
  </div>
<% end %>